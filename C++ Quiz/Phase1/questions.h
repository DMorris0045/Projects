#ifndef QUESTION_H
#define QUESTION_H
#include "node.h"

bool multipleChoice(node* q); // handles mcq questions
bool trueFalse(node* q); // handles tf questions
bool writtenAnswer(node* q); // handles written answer questoins

#endif

